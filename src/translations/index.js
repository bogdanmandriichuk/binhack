// Шляхи правильні, тому що цей файл знаходиться в тій же папці, що й JSON-файли.
import en from './en.json';
import hi from './hi.json'; // Виправлено: тепер імпортується hi.json

const translations = {
  en,
  hi,
};

export default translations;
